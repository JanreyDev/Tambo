# BCMP Web local config. Copy to .env.local
# Base URL of your local bcmp-api (php artisan serve defaults to 8000):
NEXT_PUBLIC_API_URL=http://localhost:8000
# Optional error tracking — leave empty for local dev:
NEXT_PUBLIC_SENTRY_DSN=
